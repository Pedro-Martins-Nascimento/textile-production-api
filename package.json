{
  "name": "textile-production-api",
  "version": "1.0.0",
  "scripts": {
    "setup": "node setup.js",
    "start": "node start.js",
    "stop": "node stop.js",
    "test": "node test.js",
    "playwright": "npx playwright test --config=frontend/playwright.config.js",
    "playwright:headed": "npx playwright test --headed --config=frontend/playwright.config.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}